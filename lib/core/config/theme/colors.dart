import 'package:flutter/material.dart';

const progressColor = Color(0xff5EB60F);
ColorScheme getDartScheme = const ColorScheme.dark(
  primary: Color(0xff151518), // Main BG Screens
  onPrimary: Colors.white, // Main Text
  secondary: Color(0xff650404), // TxtBtns, Hovered BG Widgets
  surface: Color(0xff26262D), // BG for Widgets
  error: Color(0xffB70F0F), // Btns BG, borders, errors
  onSurface: Color(0xff767678), // Txts inside surfaces
);